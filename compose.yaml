services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - NODE_ENV=development
    command: npm run dev
    volumes:
      - ./src:/app/src
      - /app/node_modules

