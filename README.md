## DockerSamples

Node.js + Express app with Sequelize and PostgreSQL, containerized with Docker.

### Quick start

```bash
docker compose up --build
```

The app will listen on `http://localhost:3000` by default.

